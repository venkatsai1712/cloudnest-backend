# CloudNest

CloudNest is a Spring Boot file storage API with session authentication, PostgreSQL metadata storage, and MinIO object storage.


API:

```text
http://localhost:8080
```

MinIO Console:

```text
http://localhost:9001
```

